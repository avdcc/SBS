extends ../layout

block content
  .w3-container 
    .w3-card-4
      header.w3-container.w3-blue
        h3= title
      .w3-container
        p Add the user id that you wish to predict the preferences of below
        form.w3-container(action='/hybrid' method='POST')
          input.w3-input(type='text',name="user" ,placeholder="please insert the user to predict(integer)")
          input.w3-btn.w3-green(type='submit', value='Enviar')   
      footer.w3-container.w3-blue
        p
          a(href='http://localhost:3000')
            | Back
          a= ' '
          a(href='http://localhost:3000')
            | Main menu
